$(document).ready(function()
{
	// --- load in the baby step friends content
	window.daveCodeTest.social.init();

	// --- init events & actions for navigating between steps
	window.daveCodeTest.nav.init();
});